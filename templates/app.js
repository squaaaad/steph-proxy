module.exports = (gallery, overview, sidebar, reviews, recs) => `
  <div id="gallery-app">${gallery}</div>
  <div id="overview-app">${overview}</div>
  <div id="sidebar-app">${sidebar}</div>
  <div id="reviews-app">${reviews}</div>
  <div id="recommendations-app">${recs}</div>
`;
